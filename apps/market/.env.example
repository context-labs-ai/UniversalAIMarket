# Universal AI Market (Standalone Market site)

# If set to "1", tool execution requires a Bearer token from /api/auth/verify (EIP-191 challenge-sign)
AGENT_AUTH_REQUIRED=0

# Optional: override these if you want the discovery doc to advertise a different base URL
# MARKET_PUBLIC_BASE_URL=http://localhost:3001

# Optional (frontend): use an external Agent SSE service via market proxy
# If set, the UI will start the agent stream with `engine=proxy&upstream=...`.
# NEXT_PUBLIC_AGENT_UPSTREAM=http://localhost:8080/api/agent/stream
# Optional: pass scenario=multi to drive the multi-agent demo flow in apps/agent
# NEXT_PUBLIC_AGENT_SCENARIO=multi
#
# Tip (hackathon demo): you can also start a session from terminal via `POST http://localhost:8080/api/agent/run`
# and then watch it in the market UI (Agent sidebar -> Session ID -> 观看).

# Same env vars used by settlement/config endpoints (copy from root .env.example or apps/web docs)
# BASE_GATEWAY_ADDRESS=
# BASE_USDC_ADDRESS=
# ZETA_UNIVERSAL_MARKET=
# POLYGON_WEAPON_ESCROW=
# POLYGON_MOCK_WEAPON_NFT=
# BUYER_PRIVATE_KEY=
# SELLER_PRIVATE_KEY=
# External AG-UI agent upstream (used by /api/agui/run)
AGUI_UPSTREAM_URL=http://localhost:8080/api/agui/run

# Switch UI agent transport:
# - legacy: use /api/agent/stream events (message/tool_call/timeline_step)
# - agui: use /api/agui/run (AG-UI event stream)
NEXT_PUBLIC_AGENT_ENGINE=agui
