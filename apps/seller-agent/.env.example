# Seller Agent Service

# Port for this seller agent server
PORT=8081

# Seller identity
SELLER_ID=seller-agent-a
SELLER_NAME=卖家 Agent A

# Seller style: aggressive | pro | friendly
SELLER_STYLE=aggressive

# ─── Pricing Policy ───
# List price is provided by the hub, these control discount bounds.
MIN_PRICE_FACTOR=0.80
MAX_DISCOUNT_PER_ROUND=0.06

# ─── LLM Configuration ───
# On startup, the agent tests LLM connectivity. If it fails, it falls back to deterministic templates.
# Check startup logs for: [seller-agent] LLM 连接成功 or [seller-agent] LLM 连接失败

# Model name
MODEL=qwen-plus

# API Key (required for LLM mode)
OPENAI_API_KEY=

# Base URL for OpenAI-compatible APIs (leave empty for OpenAI official)
OPENAI_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1

# ─── Common LLM Configurations ───
# Alibaba Qwen (DashScope International):
#   MODEL=qwen-plus
#   OPENAI_API_KEY=sk-your-dashscope-key
#   OPENAI_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1