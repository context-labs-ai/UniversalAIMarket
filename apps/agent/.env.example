# Agent service (LangChain) for Universal AI Market

# Port for this agent server
PORT=8080

# Where the Next.js web app is running (for calling /api/config and /api/agent/tool and /api/settle/stream)
WEB_BASE_URL=http://localhost:3000

# LLM config (OpenAI-compatible)
# OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1
MODEL=qwen-turbo

