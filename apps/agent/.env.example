# Agent service (LangChain) for Universal AI Market

# Port for this agent server
PORT=8080

# Where the Market API is running (for calling /api/config, /api/agent/tool, /api/settle/stream)
WEB_BASE_URL=http://localhost:3001

# ─── LLM Configuration ───
# On startup, the agent tests LLM connectivity. If it fails, it falls back to deterministic templates.
# Check startup logs for: [agent] LLM 连接成功 or [agent] LLM 连接失败

# Model name
MODEL=qwen-plus

# API Key (required for LLM mode)
OPENAI_API_KEY=

# Base URL for OpenAI-compatible APIs (leave empty for OpenAI official)
OPENAI_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1

# ─── Common LLM Configurations ───
# Alibaba Qwen (DashScope International):
#   MODEL=qwen-plus
#   OPENAI_API_KEY=sk-your-dashscope-key
#   OPENAI_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1

# ─── Multi-Agent Mode (scenario=multi) ───
# Start two seller-agent instances and point to their /api/seller/chat endpoints.
SELLER_AGENT_A_URL=http://localhost:8081/api/seller/chat
SELLER_AGENT_B_URL=http://localhost:8082/api/seller/chat
# Or use comma-separated list:
# SELLER_AGENT_URLS=http://localhost:8081/api/seller/chat,http://localhost:8082/api/seller/chat

# ─── Agent Auth (optional) ───
# If apps/market sets AGENT_AUTH_REQUIRED=1, this service must authenticate via EIP-191 challenge-sign.
AGENT_ID=demo-agent
AGENT_PRIVATE_KEY=
